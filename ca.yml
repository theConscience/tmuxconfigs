name: ca
root: <%= @settings["root"] || [@args[0]].compact.map { |a| File.expand_path(a) }.find { |p| File.directory?(p) } || File.expand_path("~/web_dev/repos/projects/channel-admin") %>
startup_window: editor
startup_pane: 0
enable_pane_titles: true
pane_title_position: top

windows:
  - editor:
      layout: main-vertical

      focused_pane: editor
      panes:
        - editor: nvim .
        - dev: bash -lc 'pnpm dev; exec "$SHELL"'
        - git: bash -lc 'git status; exec "$SHELL"'

  - ai:
      focused_pane: opencode
      panes:
        - opencode: bash -lc '{ if command -v opencode >/dev/null 2>&1; then opencode --continue || opencode; else printf "%s\n" "opencode is not installed"; fi; }; exec "$SHELL"'

  - check:
      focused_pane: tests
      panes:
        - tests: bash -lc 'pnpm typecheck; exec "$SHELL"'
